# Solo QA fixes — 6 September 2026

Baseline: `412b997`. User authorized the remaining audit findings; Solo must finish before Open Gambit. Changes are local and uncommitted. Previous external commits are preserved.

## Result

All 18 remaining Solo findings have local remedies. Original #14 was already resolved before this work. Independent code-reviewer verdict: APPROVE for domain, persistence, messaging and portable documents. This is a local-first application; this result does not claim a production cloud service or payment processing integration.

| Original finding | Remedy |
|---|---|
| 1 Cross-device links | Minimal per-recipient invoice/receipt snapshots in hash URLs. Recipient route mounts outside StoreProvider; fresh context needs no provider storage. Dated, unsigned, non-revocable copies are explicitly labeled. |
| 2 Installments | Canonical paidAmount/balanceDue across dashboard, reminders, client, slip, export and receipts. Settlement receipt displays invoice total while individual payment rows remain unchanged. Multi-invoice FAQ lists exact document balances. |
| 3 Silent persistence failure | Synchronous durable dispatch before UI acknowledgment/LINE. Failed writes reject changes and show a persistent error, also inside payment dialog. Broken raw JSON is preserved behind recovery UI. |
| 4 Backup validation | Nested types, references/ownership, money, dates, settlement, duplicate IDs, package carry records, receipt counter and size limits. Validator does not throw on malformed rows; restore preserves previous raw state under `solo-demo-v3-before-restore`. |
| 5 Historical pricing | Completion price snapshots, legacy snapshot before edits, billing-mode/flat-price changes blocked when unbilled work remains. Purchased or used package entitlements cannot disappear through mode changes. |
| 6 Fake real payment destination | Real mode displays a validated PromptPay destination with transfer instructions; no demo destination or fake QR. Financial sending blocked until receiver name/destination are configured. User checks bank recipient and slip manually. |
| 7 Financial deletion | Real subjects with invoices are archived; invoice/payment/receipt records remain. Inactive subjects cannot renew packages. Outstanding bills stay actionable. |
| 8 Package carry loss | Editing preserves purchasedAt and carriedUnitIds in both UI and reducer. |
| 9 Cancelled FAQ schedule | Next-session answer excludes cancelled/inactive work. |
| 10 CSV formula injection | Dangerous leading characters are neutralized after whitespace/control characters. |
| 11 CSV correctness/download | One file, explicit cancellation, cumulative paid/balance and settlement date. |
| 12 Vulnerable dependencies | Existing router upgraded to 7.18.3, Vite floor 6.4.3 and Vitest 3.2.7. Production and development audits both clean. |
| 13 Profession coupling | Profession-owned templates and mode labels, generic fallback and contract tests. README accurately limits currently supported UI to tutors. |
| 15 Domain validation | Finite positive safe integers, actual ISO dates/times, coherent money and reducer guards; rejected actions do not report successful persistence. |
| 16 Contact console log | Removed contact/name console logging. |
| 17 Modal accessibility | Portal, focus trap, inert background, labeled heading, nested modal management and restoration. |
| 18 Progress accessibility | Exposed label/range/value semantics. |
| 19 Stale documentation | Removed hardcoded test counts and obsolete profession/demo claims. |

Additional review corrections: edited financial drafts retain user prose but are blocked when underlying data changes; explicit regeneration replaces stale financial content. Compact change fingerprints avoid copying recipient history into every draft. Mobile attendance names now have room above action buttons.

## Validation

- Unit: 196 passed across 25 files.
- Full Playwright: 46 passed on Chromium mobile and desktop.
- Latest financial regression after final domain changes: 8 passed.
- Production build/TypeScript: passed.
- `npm audit` including development dependencies: 0 vulnerabilities.
- Visual sweep: 33 screenshots (11 routes × 320/390/1440px), no horizontal overflow or console/page errors. Narrow attendance layout inspected again after final CSS change.
- Independent reviewer: APPROVE; final targeted billing/delivery tests passed.

Evidence: `/Users/tasachi/Documents/qa-fixes-2026-09-06/solo/` (logs, `screenshots.json`, `screenshots.html`). Prior audit remains at `/Users/tasachi/Documents/qa-audit-2026-09-05/`.

UX assessment for the local-first tutor workflow: **8/10 UX, 8/10 UI**. Payment/recovery clarity and keyboard access improved. Desktop still uses the chosen phone-style frame by default; the menu offers web view. These are review judgments, not automated metrics.

## Product boundaries

No account system, cloud synchronization, live/signed/revocable document links, automatic bank verification, or real QR generator was added. The safe local remedy for sharing is a dated read-only copy; for payment it is a validated destination with clear transfer instructions. Those cloud/provider capabilities require a separate architecture and external setup. No production deployment or external payment took place.

Upgrade references: [Router advisories](https://github.com/advisories/GHSA-wrjc-x8rr-h8h6), [Vitest advisory](https://github.com/advisories/GHSA-5xrq-8626-4rwp), [Vite advisory](https://github.com/advisories/GHSA-v6wh-96g9-6wx3).
